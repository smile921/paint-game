<div class="conatinder">
  <div class="pin fiexed flex-col p-2">
    <div class="flex flex-shink min-h-16">
      <div class="flex-1 bg-black text-white">
        <button 
          class="flex-shrink w-1/4 bg-pink hover:bg-pink-light text-white border-pink-lighter font-bold py-2 px-4 rounded"
          onclick={{action 'newGame'}}
        >
          🎲 New Game
        </button>
      </div>
      <div class="flex-1 p-4 bg-pink">
        {{!-- {{color}} --}}
        <ColorPicker @color={{color}} @onChange={{action (mut color)}} />
      </div>
    </div>
    <div class="flex m-4 bg-gray-lighter">
      <div class="flex-1 border-r-2 border-gray-dark border-dashed p-4">
        <div class="p-16">
          <PictureGrid @picture={{game.templatePicture}} />
        </div>
      </div>
      <div class="flex-1 p-4">
        <div class="p-8">
          <PictureGrid 
            @picture={{game.gamePicture}} 
            @color={{color}}
            @isEditable={{true}}
            @onChange={{action 'changeColor'}}
          />
        </div>
      </div>
    </div>
  </div>
</div>
{{outlet}}